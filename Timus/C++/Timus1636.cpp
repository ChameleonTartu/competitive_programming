/*
#include <iostream>

using namespace std;

int main() {
	int one, two, sum = 0, failure; cin >> one >> two;
	
	for (int i = 0; i < 10; ++i)
	{
		cin >> failure;
		sum += failure * 20;
	}
	if (two - sum < one)
		cout << "Dirty debug :(" << endl;
	else cout << "No chance." << endl;
	//while (1);
	return 0;
}
*/