/*
#include <iostream>

using namespace std;

int main() {
	int points; cin >> points;
	int points_sum = 0, current_point;
	for (int i = 0; i < points; ++i) {
		cin >> current_point;
		points_sum += current_point;
	}
	cout.precision(6);
	cout << fixed << (double)points_sum / points << endl;
	return 0;
}
*/